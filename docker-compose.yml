version: '3'

services:
  postgres:
    image: postgres:11.1-alpine
    ports:
      - 5432:5432
    environment:
      POSTGRES_DB: no
      POSTGRES_USER: no
      POSTGRES_PASSWORD: no
    volumes:
      - "db:/var/lib/postgresql/data/"

volumes:
  db:
